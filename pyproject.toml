[build-system]
requires = ["setuptools>=75.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cicd-learning"
version = "0.1.0"
description = "Learning CI/CD concepts with practical examples"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
